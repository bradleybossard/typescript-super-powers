"use strict";
var messyArray = ['something', 2, true, undefined, null];
var randomStrings = ['one', 'two', 'three'];
var tuple = [24, 'what', 'e'];
function sayMyName(name) {
    return name;
}
// sayMyName(32);  // error
console.log(sayMyName('heather'));
